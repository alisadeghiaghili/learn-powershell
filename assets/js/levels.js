/**
 * Curriculum index: series catalog + level aggregator.
 * Series modules are appended here; keep IDs stable for progress/share.
 */

export { LEVELS, SERIES, getLevel, levelsInSeries, seriesTitle, nextLevelId, evaluateGoal } from "./levels-catalog.js";
